//
// Constants.js
//
var Constants = {

	/**
	 * [KEY_TO_CHECK : name of the field that can contain tags information
	 * @type {String}
	 */
    KEY_TO_CHECK : 'tags',

	/**
	 * [TAGS_SEARCH : name of program which is used to compute sha for cache files]
	 * @type {String}
	 */
    TAGS_SEARCH  : 'node_tags_search'
};

exports = module.exports = Constants;